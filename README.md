# Elysium Protocol Buffers API

**The official Elysium protocol buffers API.**

## Rust API

The Rust API resides in the `elysium_rust` directory as the `elysium_rust` crate.

## Dart API

The Dart API resides in the `elysium_dart` directory as the `elysium_dart` package.

